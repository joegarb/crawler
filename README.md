# Crawler

A simple web crawler written in Java.

## Prerequisites

- Java 21 or later

## Building

```bash
./mvnw clean package
```

On Windows, use `mvnw.cmd` instead.

## Running

```bash
./crawl <startUrl>
```
