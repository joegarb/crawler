#!/bin/bash

START_URL="$1"

JAR="target/crawler-1.0.jar"

if [ ! -f "$JAR" ]; then
  echo "JAR not found. Building..."
  ./mvnw clean package
  if [ $? -ne 0 ]; then
    echo "Build failed!"
    exit 1
  fi
fi

java ${LOG_LEVEL:+-DLOG_LEVEL=$LOG_LEVEL} -jar "$JAR" ${START_URL:+"$START_URL"}
